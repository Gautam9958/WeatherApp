import type {WeatherData} from '../types/weather'; import {weatherIcon,weatherLabel} from '../utils/weatherCodes'; import {temp,useWeatherStore} from '../store/weatherStore'; import {Droplets,Wind,Sun,Eye} from 'lucide-react';
export default function WeatherCard({weather}:{weather:WeatherData}){const {location,unit}=useWeatherStore();const c=weather.current;return <section className="hero card"><div><p className="eyebrow">CURRENT WEATHER</p><h2>{location.name}</h2><p className="muted">{location.country}</p><div className="hero-temp">{weatherIcon(c.weather_code)} <b>{temp(c.temperature_2m,unit)}°</b><span>{unit}</span></div><p className="condition">{weatherLabel(c.weather_code)} · Feels like {temp(c.apparent_temperature,unit)}°</p></div><div className="stats"><div><Droplets/><span>Humidity</span><b>{c.relative_humidity_2m}%</b></div><div><Wind/><span>Wind</span><b>{Math.round(c.wind_speed_10m)} km/h</b></div><div><Sun/><span>Precipitation</span><b>{c.precipitation} mm</b></div><div><Eye/><span>Daylight</span><b>{c.is_day?'Day':'Night'}</b></div></div></section>}
