import {useState} from 'react'; import {Bot,Send,LoaderCircle} from 'lucide-react'; import {askOllama} from '../services/ollamaApi'; import {useWeatherStore} from '../store/weatherStore'; import type {ChatMessage} from '../types/weather';
export default function WeatherAI(){const weather=useWeatherStore(s=>s.weather);const [messages,setMessages]=useState<ChatMessage[]>([]);const [q,setQ]=useState('');const [loading,setLoading]=useState(false);const send=async()=>{if(!q.trim()||!weather||loading)return;const question=q.trim();setQ('');setMessages(m=>[...m,{role:'user',content:question}]);setLoading(true);try{const answer=await askOllama(question,weather,messages);setMessages(m=>[...m,{role:'assistant',content:answer}])}catch(e:any){setMessages(m=>[...m,{role:'assistant',content:e.message}])}finally{setLoading(false)}};const suggestions=['Will it rain today?','Is it good for a bike ride?','Compare today and tomorrow','Explain today’s weather'];return <section className="ai card"><div className="ai-title"><div className="ai-icon"><Bot/></div><div><p className="eyebrow">LOCAL AI</p><h3>Weather AI</h3><span className="muted">Powered by Ollama</span></div></div><div className="chat">{messages.length===0?<div className="empty-chat"><Bot size={36}/><p>Ask questions about the current forecast.</p><div className="suggestions">{suggestions.map(x=><button onClick={()=>setQ(x)} key={x}>{x}</button>)}</div></div>:messages.map((m,i)=><div className={'msg '+m.role} key={i}><b>{m.role==='user'?'You':'AI'}</b><p>{m.content}</p></div>)}{loading&&<div className="msg assistant"><LoaderCircle className="spin" size={18}/><p>Analyzing forecast…</p></div>}</div><div className="composer"><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Ask Weather AI anything…"/><button onClick={send}><Send size={18}/></button></div></section>}
